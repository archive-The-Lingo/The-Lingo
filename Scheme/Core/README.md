# Core

superset of CoreExp

not subset of r7rs (`#%define-macro`)

## Extra Syntax

+ `(begin x ...)`
+ `(define p x)`
+ `(define-record-type name constructor pred field ...)` (no modifier)
+ `(#%define-macro p x)`
+ `(quasiquote x)`
