# internal-record

depends on [integer](integer)

## Type

+ ?*record

## Procedure

+ `(?*record? x)`
+ `(?*record name fields)` fields: list
+ `(?*record-name x)`
+ `(?*record-fields x)`
+ `(?*record-field-ref x i)`