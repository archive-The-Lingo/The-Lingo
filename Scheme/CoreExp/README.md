# CoreExp

superset of MinimalExp

not subset of r7rs (`#%record`)

## Extra Types

+ promise
+ #%record

## Extra Syntax

+ `(delay x)`

## Extra Procedures

+ `(force x)`
+ `(promise? x)`

+ `(#%record? x)`
+ `(#%record name fields)` fields: list
+ `(#%record-name x)`
+ `(#%record-fields x)`
+ `(#%record-field-ref x i)`
